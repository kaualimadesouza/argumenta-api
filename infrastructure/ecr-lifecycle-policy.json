{
  "rules": [
    {
      "rulePriority": 1,
      "description": "Keep only the last 10 images (each deploy pushes a new commit-SHA tag; nothing expires them on its own)",
      "selection": {
        "tagStatus": "any",
        "countType": "imageCountMoreThan",
        "countNumber": 10
      },
      "action": {
        "type": "expire"
      }
    }
  ]
}
