__pycache__/
*.pyc
.venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

.env
.env.*
!.env.example

.neon

# terraform state and local working dirs; state lives in the remote S3 backend,
# never in git (it contains the resolved secret values)
infrastructure/.terraform/
infrastructure/*.tfstate
infrastructure/*.tfstate.*
infrastructure/*.tfvars
infrastructure/crash.log

.DS_Store
.idea/
.vscode/

# board dumps from `gh project item-list`, never part of the source
project_items.json
signoz/data/
